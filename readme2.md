Montando a estrutura do projeto:

src/
├── controllers/      # Controladores que lidam com as rotas
├── models/           # Modelos que interagem com o banco de dados
├── routes/           # Rotas que conectam o controlador com as requisições
├── services/         # Lógica de negócio
└── app.js            # Arquivo principal que configura o servidor


